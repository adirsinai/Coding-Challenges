//challenge #2
if (markHigherBmi) {
    console.log(`"Mark's BMI (${markBmi}) is higher than than john's (${johnBmi})`)
} else {
    console.log(`"john's BMI (${johnBmi}) is higher than than Mark's (${markBmi})`)
}
